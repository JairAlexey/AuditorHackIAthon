# URL del backend desplegado (FastAPI)
# En desarrollo local se deja vacío (el proxy de Vite redirige /api al localhost:8000)
# En Cloudflare Pages se configura en: Settings → Environment Variables
VITE_API_URL=https://tu-backend.onrender.com
